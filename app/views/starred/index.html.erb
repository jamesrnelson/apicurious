<h1>Starred <%= @github_starred.user_repos.length %></h1>
<% @github_starred.user_repos.each do |repo| %>
  <p><%= "#{repo.owner}/#{repo.name}" %></p>
  <p><%= repo.description %></p>
  <p><%= "#{repo.language} #{repo.forks_count} #{repo.stargazers_count}" %></p>
<% end %>
